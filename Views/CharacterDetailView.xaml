<UserControl x:Class="GuardianOS.Views.CharacterDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GuardianOS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#141414">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Detalles del Personaje" 
                       FontSize="32" 
                       Foreground="White" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>
            
            <TextBlock FontSize="24" Foreground="#F5DC56" HorizontalAlignment="Center">
                <Run Text="{Binding Character.ClassName, Mode=OneWay}"/>
                <Run Text=" - "/>
                <Run Text="{Binding Character.Light}"/>
                <Run Text="✦"/>
            </TextBlock>
            
            <TextBlock Text="{Binding Character.CharacterId}"
                       FontSize="14"
                       Foreground="#888888"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
