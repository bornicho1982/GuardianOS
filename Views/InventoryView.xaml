<UserControl x:Class="GuardianOS.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GuardianOS.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header / Filtros (Futuro) -->
        <Border Grid.Row="0" Margin="0,0,0,16" Background="#161616" CornerRadius="8" Padding="16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Inventario" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#888888" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Area Principal de Items -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
             <ItemsControl ItemsSource="{Binding FilteredItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="64" Height="64" Margin="4" Background="#262626" CornerRadius="4" ToolTip="{Binding Name}">
                            <Grid>
                                <!-- Imagen Icono -->
                                <Image Source="{Binding IconUrl}" Stretch="UniformToFill" Margin="2"/>
                                <!-- Overlay Poder -->
                                <Border Background="#AA000000" VerticalAlignment="Bottom" Height="16">
                                     <TextBlock Text="{Binding PrimaryStatValue}" 
                                                Foreground="#F5DC56" 
                                                FontSize="10" 
                                                HorizontalAlignment="Right" 
                                                Margin="0,0,4,0"
                                                FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
             </ItemsControl>
        </ScrollViewer>
        
        <!-- Loading Indicator -->
        <Grid Grid.RowSpan="2" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}" d:IsHidden="True">
             <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                 <materialDesign:PackIcon Kind="Loading" Width="48" Height="48" Foreground="#F5DC56">
                      <materialDesign:PackIcon.RenderTransform>
                         <RotateTransform CenterX="24" CenterY="24"/>
                      </materialDesign:PackIcon.RenderTransform>
                      <materialDesign:PackIcon.Triggers>
                         <EventTrigger RoutedEvent="Loaded">
                             <BeginStoryboard>
                                 <Storyboard>
                                     <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever"/>
                                 </Storyboard>
                             </BeginStoryboard>
                         </EventTrigger>
                      </materialDesign:PackIcon.Triggers>
                 </materialDesign:PackIcon>
                 <TextBlock Text="Cargando Inventario..." Foreground="White" Margin="0,16,0,0" HorizontalAlignment="Center"/>
             </StackPanel>
        </Grid>
    </Grid>
</UserControl>
